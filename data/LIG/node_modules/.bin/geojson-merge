#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojson-merge/geojson-merge" "$@"
  ret=$?
else 
  node  "$basedir/../geojson-merge/geojson-merge" "$@"
  ret=$?
fi
exit $ret
